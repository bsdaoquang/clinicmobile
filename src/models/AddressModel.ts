export interface AddressModel {
  city: string;
  countryCode: string;
  countryName: string;
  county: string;
  district: string;
  label: string;
  postalCode: string;
  street: string;
  houseNumber?: string;
}
